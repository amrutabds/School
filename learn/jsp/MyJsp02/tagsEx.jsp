<HTML>
<BODY>
<H1>Going to include hello.jsp...</H1><BR>
<jsp:include page="hello.jsp"/>
</BODY>
</HTML>

<!--JSP tags do not use , but just the < character.  A JSP tag is somewhat like an HTML tag.  
JSP tags can have a "start tag", a "tag body" and an "end tag".  The start and end tag both use the tag name,
 enclosed in < and > characters.  The end starts with a / character after the < character.  The tag names 
have an embedded colon character : in them, the part before the colon describes the type of the tag.- ->